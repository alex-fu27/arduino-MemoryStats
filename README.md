Memory usage statistics for AVR-based Arduino boards.
PlatformIO library.

Uses internal variables of AVR-libc, so may break at some time.

See [C header](include/MemoryStats.h) for available methods.
